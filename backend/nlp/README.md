# NLP Modules

- `arabic/`: Arabic NLP layer.

Current status:

- Phase 1 implemented (normalization, tokenization, fuzzy matching, bridge rules).
- Integrated into `backend/ai_core.py`.
- Phase 2 planned for dialect and deeper grammar reasoning.
